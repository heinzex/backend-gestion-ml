server:
  port: 8001
spring:
  application:
    name: token-service
  jpa:
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASS}
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
  cloud:
    kubernetes:
      config:
        name: token-service # Nombre de tu servicio
        namespace: default # El namespace en el que tu servicio se está ejecutando
      discovery:
        all-namespaces: true # Si los servicios pueden descubrirse entre diferentes namespaces
    loadbalancer:
      ribbon:
        enabled: false


  